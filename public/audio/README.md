# Optional background audio

The card uses a built-in soft ambient tone (Web Audio) — tap the **speaker icon** bottom-left to turn it on or off.

To use your own MP3 instead, replace the `BackgroundAudio` component to point at `ambient.mp3` in this folder.
